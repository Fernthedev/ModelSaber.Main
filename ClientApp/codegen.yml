overwrite: true
schema: "https://localhost:44300/graphql"
documents: "graphql/*.graphql"
generates:
  src/graphqlTypes.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
